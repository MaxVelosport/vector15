--- server/index.ts
+++ server/index.ts
@@ -10,6 +10,8 @@
 import { serveStatic } from "./static";
 import { createServer } from "http";
 import { seedDemoAccountIfNotExists } from "./seed-demo-auto";
+import * as dotenv from 'dotenv';
+dotenv.config();
 
 const app = express();
 const httpServer = createServer(app);
